# Qwen Max 模型升级

## 目标

将 My-Notion 运行时允许使用的 Qwen Max 模型从 `qwen3.7-max-2026-06-08` 升级为 `qwen3.8-max`，保持 Web 模型选择器与共享 AI 后端配置一致。

## 完成内容

- 更新 `packages/ai/config/model.ts` 中的模型白名单、实际模型映射、展示名称和模型配置。
- 更新 `apps/web/src/components/ai-chat/models.ts` 中的 Web 模型白名单与展示名称。
- 增加配置测试，明确验证 `qwen3.8-max` 可用、旧 `qwen3.7-max-2026-06-08` 已被拒绝。
- 历史进展、博客和外部平台兼容性参考文档保持不变，避免篡改历史记录或未经验证的兼容性资料。

## 验证

- `pnpm test packages/ai/__tests__/config.test.ts`：通过，13 个测试全部成功。
- `pnpm --filter @notion/web typecheck`：通过。
- `pnpm --filter @notion/web build`：通过。
- `pnpm --filter @notion/web lint`：通过，0 error；保留 7 个与本次修改无关的既有 warning。

## 风险与边界

- 本次只更新模型 ID，不改变默认模型；默认模型仍为 `kimi-k2.7-code`。
- 已保存旧模型 ID 的浏览器会在初始化时回退到默认模型，因为旧 ID 不再属于允许列表。
- 未执行真实 DashScope 请求，本次验证覆盖静态配置、类型、构建和白名单行为。

## 下一步

在具备可用 DashScope 凭据的环境中，可补一次 `qwen3.8-max` 的真实聊天与工具调用 smoke，确认平台侧模型权限和 function calling 行为。
